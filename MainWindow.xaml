<Window x:Class="HollyJson.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HollyJson.ViewModels"
        xmlns:models="clr-namespace:HollyJson.Models"
        mc:Ignorable="d" ResizeMode="NoResize"
        Icon="/main.ico"
        Title="Holly JsonEditor" Height="710" Width="600">
    <Window.DataContext>
        <local:MainModel/>
    </Window.DataContext>
    <Window.Resources>
        <local:DateTimeToDateConverter x:Key="myDateConverter" />
        <local:LangStringConverter x:Key="LocaleConverter" />
        <Style TargetType="{x:Type TextBox}" x:Key="txtbox">
            <Setter Property="IsEnabled" Value="{Binding RelativeSource={RelativeSource Mode=FindAncestor,AncestorType={x:Type Window}}, Path=DataContext.Save_Loaded}" />
            <EventSetter Event="TextChanged" Handler="TextBox_TextChanged"/>
            <EventSetter Event="DataObject.Pasting" Handler="PastingHandler"/>
        </Style>
        <models:Character x:Key="Character"/>
    </Window.Resources>
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Label Content="{Binding Info.studioName}" Grid.Row="0" HorizontalAlignment="Left" VerticalContentAlignment="Center" FontWeight="Bold"/>
            <Label Content="{Binding SelectedChar.portraitBaseId}" HorizontalAlignment="Center"/>
            <Label Content="{Binding SelectedChar.gender}" HorizontalAlignment="Center" Margin="123,0,0,0"/>
            <Label Content="{Binding Info.Now, Converter={StaticResource myDateConverter}}" Grid.Row="0" HorizontalAlignment="Right" VerticalContentAlignment="Center" FontWeight="Bold"/>
            <Grid Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="3" Margin="0,5,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Label Content="Budget" VerticalContentAlignment="Center"  Grid.Column="0"/>
                <TextBox Text="{Binding Info.budget}" VerticalContentAlignment="Center" Height="22"
                         PreviewTextInput="NumberValidation" TextChanged="TextBox_TextChanged" Grid.Column="1"/>
                <Label Content="Cash" VerticalContentAlignment="Center" Grid.Column="2"/>
                <TextBox Text="{Binding Info.cash}" VerticalContentAlignment="Center" Height="22"
                         PreviewTextInput="NumberValidation" TextChanged="TextBox_TextChanged" Grid.Column="3"/>
                <Label Content="Reputation" VerticalContentAlignment="Center"  Grid.Column="4"/>
                <TextBox Text="{Binding Info.reputation, StringFormat={}{0:0.0}}" VerticalContentAlignment="Center" Height="22"
                         PreviewTextInput="DoubleValidation" TextChanged="TextBox_TextChanged" Grid.Column="5"/>
                <Label Content="Influence" VerticalContentAlignment="Center"  Grid.Column="6"/>
                <TextBox Text="{Binding Info.influence}" VerticalContentAlignment="Center" Height="22"
                         PreviewTextInput="NumberValidation" TextChanged="TextBox_TextChanged"  Grid.Column="7"/>
            </Grid>
            <Button Command="{Binding OpenFileCmd}" CommandParameter="Names" Grid.Column="1" Height="25" Width="25" Margin="5,0,0,0">
                <Button.Content>
                    <Image Source="/Resources/user.png"/>
                </Button.Content>
                <Button.ToolTip>
                    <TextBlock Padding="0" Text="Open CHARACTER_NAMES file for better usability"/>
                </Button.ToolTip>
            </Button>
            <Button Command="{Binding OpenFileCmd}" CommandParameter="OFD" Grid.Column="2" Height="25" Width="25" Margin="5,0,0,0">
                <Button.Content>
                    <Image Source="/Resources/ofd.png"/>
                </Button.Content>
                <Button.ToolTip>
                    <TextBlock Padding="0" Text="Open save.json file"/>
                </Button.ToolTip>
            </Button>
        </Grid>
        <Grid Grid.Row="1" Grid.Column="0" Margin="5,5,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="60"/>
            </Grid.ColumnDefinitions>
            <ComboBox Margin="0,0,5,5" ItemsSource="{Binding StudioList}" SelectedItem="{Binding Filter_studio}">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding ., Converter={StaticResource LocaleConverter}}" Padding="0" Margin="5,0,0,0" TextTrimming="CharacterEllipsis"/>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
            <TextBox Grid.Column="1" Margin="0,0,5,5" Text="{Binding Filter_txt, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            <ComboBox Grid.Column="2" Margin="0,0,0,5" ItemsSource="{Binding ProfList}" SelectedItem="{Binding Filter_Prof}" 
                      ToolTip="{Binding RelativeSource={RelativeSource Self}, Path=Text, Converter={StaticResource LocaleConverter}}">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding ., Converter={StaticResource LocaleConverter}}" Padding="0" Margin="5,0,0,0" TextTrimming="CharacterEllipsis"/>

                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
            <ListView Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="3" ItemsSource="{Binding Filtered_Obj}" 
          SelectionMode="Single" SelectedItem="{Binding SelectedChar}" AlternationCount="2" Background="White">
                <ListView.Resources>
                    <Style TargetType="ListViewItem">
                        <Style.Triggers>
                            <Trigger Property="ItemsControl.AlternationIndex"  Value="0">
                                <Setter Property="Background" Value="#3F50d5fa" />
                            </Trigger>
                            <Trigger Property="ItemsControl.AlternationIndex"  Value="1">
                                <Setter Property="Background" Value="#3FFFA64C" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="true" >
                                <Setter Property="Background" Value="#4BFC80" />
                                <Setter Property="FontWeight" Value="Bold" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </ListView.Resources>
                <ListView.ItemTemplate>
                    <DataTemplate DataType="local:Character">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Label Content="{Binding MyCustomName}" HorizontalAlignment="Left" VerticalAlignment="Center" Padding="0" Margin="0,0,5,0"
                                   Grid.Column="0"/>
                            <TextBlock Text="{Binding professions.ProfToDecode, Converter={StaticResource LocaleConverter}}" Padding="0" Grid.Column="1" 
                                   HorizontalAlignment="Right" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
        <Grid Grid.Row="1" Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="95"/>
                <RowDefinition Height="140"/>
                <RowDefinition Height="80"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.Resources>
                <Style TargetType="{x:Type Label}">
                    <Setter Property="Margin" Value="5"/>
                    <Setter Property="VerticalContentAlignment" Value="Center"/>
                    <Setter Property="HorizontalContentAlignment" Value="Right"/>
                    <Setter Property="Padding" Value="0"/>
                </Style>
                <Style TargetType="{x:Type TextBox}" BasedOn="{StaticResource txtbox}">
                    <Setter Property="Margin" Value="5"/>
                </Style>
            </Grid.Resources>
            <Border  Grid.Row="0" BorderThickness="1.5" BorderBrush="DarkGray" CornerRadius="5" Margin="5,5,0,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions >
                        <ColumnDefinition Width="60"/>
                        <ColumnDefinition Width="75"/>
                        <ColumnDefinition Width="60"/>
                        <ColumnDefinition Width="75"/>
                    </Grid.ColumnDefinitions>
                    <Label Content="Name" Grid.Row="0" Grid.Column="0"/>
                    <TextBox Text="{Binding SelectedChar.MyCustomName}" Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="3"
                           HorizontalContentAlignment="Left"  Margin="12,5,12,5" />
                    <Label Content="Studio Id" Grid.Row="1" Grid.Column="0"/>
                    <ComboBox SelectedItem="{Binding SelectedChar.studioId}" Grid.Row="1" Grid.Column="1" Height="20" Margin="13,0,13,0"
                             Padding="0" Grid.ColumnSpan="3" ItemsSource="{Binding StudioListForChar}" VerticalContentAlignment="Center">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding ., Converter={StaticResource LocaleConverter}}" Padding="0" Margin="5,0,0,0"/>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>

                    </ComboBox>
                    <Label Content="Mood"  Grid.Row="2" Grid.Column="0" />
                    <TextBox Text="{Binding SelectedChar.mood, StringFormat={}{0:0.00}}" Grid.Row="2" Grid.Column="1" Width="50" 
                             PreviewTextInput="DoubleValidation" DataObject.Pasting="PastingHandler"/>
                    <Label Content="Attitude" Grid.Row="2" Grid.Column="2"/>
                    <TextBox Text="{Binding SelectedChar.attitude, StringFormat={}{0:0.00}}" Grid.Row="2" Grid.Column="3" Width="50"
                             PreviewTextInput="DoubleValidation" DataObject.Pasting="PastingHandler"/>
                </Grid>
            </Border>
            <Border Grid.Row="1" BorderThickness="1.5" BorderBrush="DarkGray" CornerRadius="5" Margin="5,5,0,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions >
                        <ColumnDefinition Width="60"/>
                        <ColumnDefinition Width="75"/>
                        <ColumnDefinition Width="60"/>
                        <ColumnDefinition Width="75"/>
                    </Grid.ColumnDefinitions>
                    <Label Content="Prof." Grid.Row="0" Grid.Column="0" HorizontalContentAlignment="Right"/>
                    <TextBlock Text="{Binding SelectedChar.professions.ProfToDecode, Converter={StaticResource LocaleConverter}}" Grid.Row="0" Grid.Column="1" Width="50"
                             TextTrimming="CharacterEllipsis" VerticalAlignment="Center" ToolTip="{Binding RelativeSource={RelativeSource Self}, Path=Text}"/>
                    <Label Content="Age"  Grid.Row="0" Grid.Column="2"/>
                    <TextBox Text="{Binding SelectedChar.Age}" Grid.Row="0" Grid.Column="3" Width="50" 
                             PreviewTextInput="NumberValidation" DataObject.Pasting="PastingHandler"/>
                    <Label Content="Skill" Grid.Row="1" Grid.Column="2"/>
                    <TextBox Text="{Binding SelectedChar.professions.Value, StringFormat={}{0:0.0}}" Grid.Row="1" Grid.Column="3" 
                             Width="50" PreviewTextInput="LimitValidation" DataObject.Pasting="PastingHandler"/>
                    <Label Content="Limit" Grid.Row="1" Grid.Column="0"/>
                    <TextBox Text="{Binding SelectedChar.limit, StringFormat={}{0:0.0}}" Grid.Row="1" Grid.Column="1" 
                             Width="50" PreviewTextInput="LimitValidation" DataObject.Pasting="PastingHandler"/>
                </Grid>
            </Border>
            <Border Grid.Row="2" BorderThickness="1.5" BorderBrush="DarkGray" CornerRadius="5" Margin="5,5,0,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions >
                        <ColumnDefinition Width="60"/>
                        <ColumnDefinition Width="75"/>
                        <ColumnDefinition Width="60"/>
                        <ColumnDefinition Width="75"/>
                    </Grid.ColumnDefinitions>
                    <Label Grid.Row="0" Content="Contract" Grid.ColumnSpan="2" HorizontalContentAlignment="Center"/>
                    <Label Grid.Row="0" Grid.Column="2" Content="Days Left"/>
                    <TextBox Grid.Row="0" Grid.Column="3" Width="50" 
                             PreviewTextInput="NumberValidation" DataObject.Pasting="PastingHandler">
                        <TextBox.Style>
                            <Style TargetType="TextBox" BasedOn="{StaticResource txtbox}">
                                <Setter Property="Margin" Value="5"/>
                                <Setter Property="Text" Value="{Binding SelectedChar.contract.DaysLeft}"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SelectedChar.contract.contractType}" Value="2">
                                        <Setter Property="IsEnabled" Value="False"/>
                                        <Setter Property="Text" Value="∞"/>
                                        <Setter Property="Background" Value="#1F777777"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBox.Style>
                    </TextBox>
                    <Label Grid.Row="1" Grid.Column="0" Content="Years"/>
                    <TextBox Text="{Binding SelectedChar.contract.amount}" Grid.Row="1" Grid.Column="1" Width="50"
                             PreviewTextInput="NumberValidation" DataObject.Pasting="PastingHandler"/>
                    <Label Grid.Row="1" Grid.Column="2" Content="Init fee"/>
                    <TextBox Text="{Binding SelectedChar.contract.initialFee}" Grid.Row="1" Grid.Column="3" Width="50"
                             PreviewTextInput="NumberValidation" DataObject.Pasting="PastingHandler"/>
                    <Label Grid.Row="2" Grid.Column="0" Content="M/s">
                        <Label.ToolTip>
                            <TextBlock Text="Mouth salary" Padding="0"/>
                        </Label.ToolTip>
                    </Label>
                    <TextBox Text="{Binding SelectedChar.contract.monthlySalary}" Grid.Row="2" Grid.Column="1" Width="50"
                             PreviewTextInput="NumberValidation" DataObject.Pasting="PastingHandler">
                        <TextBox.ToolTip>
                            <TextBlock Text="Mouth salary" Padding="0"/>
                        </TextBox.ToolTip>
                    </TextBox>
                    <Label Grid.Row="2" Grid.Column="2" Content="W/s">
                        <Label.ToolTip>
                            <TextBlock Text="Weight salary" Padding="0"/>
                        </Label.ToolTip>
                    </Label>
                    <TextBox Text="{Binding SelectedChar.contract.weightToSalary}" Grid.Row="2" Grid.Column="3" Width="50"
                             PreviewTextInput="NumberValidation" DataObject.Pasting="PastingHandler">
                        <TextBox.ToolTip>
                            <TextBlock Text="Weight salary" Padding="0"/>
                        </TextBox.ToolTip>
                    </TextBox>
                </Grid>
            </Border>
            <Border Grid.Row="3" BorderThickness="1.5" BorderBrush="DarkGray" CornerRadius="5" Margin="5,5,0,0">
                <Grid Margin="0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Label Grid.Column="0" Content="Traits"  VerticalAlignment="Center"  Margin="10,0,0,0"
                               HorizontalAlignment="Left" Padding="0"/>
                    <ComboBox Grid.Column="1" Margin="43,5,5,5" VerticalAlignment="Center" Padding="0" Height="20" x:Name="TraitCmb" 
                              ItemsSource="{Binding Source={StaticResource Character}, Path=Labels }" VerticalContentAlignment="Center">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding ., Converter={StaticResource LocaleConverter}}" Padding="0" Margin="5,0,0,0"/>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                    <Button Grid.Column="2" Grid.Row="0" Height="20" Width="20" HorizontalAlignment="Right" 
                            VerticalAlignment="Top" Margin="10,5,10,0" Command="{Binding AddTraitCmd}" CommandParameter="{Binding ElementName=TraitCmb, Path=SelectedItem}">
                        <Button.Content>
                            <Image Source="/Resources/plus.png"/>
                        </Button.Content>
                    </Button>
                    <ScrollViewer Margin="5,5,10,5" VerticalAlignment="Stretch" Grid.Row="1" Grid.ColumnSpan="3">
                        <ItemsControl ItemsSource="{Binding SelectedChar.labels}" VerticalAlignment="Top">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="5,0,10,5">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Button Grid.Column="0" Width="18" Height="18" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" 
                                                VerticalAlignment="Center" HorizontalAlignment="Center" 
                                                Command="{Binding RelativeSource={RelativeSource Mode=FindAncestor,AncestorType={x:Type Window}}, Path=DataContext.RemoveTraitCmd}" 
                                                CommandParameter="{Binding }">
                                            <Button.Content>
                                                <Image Source="/Resources/minus.png" Margin="1,0,0,0"/>
                                            </Button.Content>
                                        </Button>
                                        <TextBlock Text="{Binding ., Converter={StaticResource LocaleConverter}}" TextWrapping="Wrap" Grid.Column="1" Margin="5,0,5,0"
                                            HorizontalAlignment="Left" Padding="0" TextAlignment="Left"/>
                                    </Grid>
                                    
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </Border>
            <Border Grid.Row="5" BorderThickness="1.5" BorderBrush="DarkGray" CornerRadius="5" Margin="5,5,0,0">
                <Grid Margin="0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Label Grid.Row="0" Content="Knowed Sins" VerticalAlignment="Top"
                               HorizontalAlignment="Center" Padding="0" Margin="5,5,0,0"/>
                    <ScrollViewer Margin="5,5,10,5" VerticalAlignment="Stretch" Grid.Row="1">
                        <ItemsControl ItemsSource="{Binding SelectedChar.aSins}" VerticalAlignment="Top">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate DataType="local:Character">
                                    <TextBlock Text="{Binding ., Converter={StaticResource LocaleConverter}}" TextWrapping="Wrap" Margin="5,0,10,5"
                                        HorizontalAlignment="Left" Padding="0" TextAlignment="Left"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </Border>
            <Border Grid.Row="4" BorderThickness="1.5" BorderBrush="DarkGray" CornerRadius="5" Margin="5,5,0,0">
                <Grid Margin="0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Label Grid.Column="0" Content="Skills"  VerticalAlignment="Center"  Margin="10,0,0,0"
                               HorizontalAlignment="Left" Padding="0"/>
                    <ComboBox Grid.Column="1" Margin="44,5,5,5" VerticalAlignment="Center" Padding="0" Height="20" VerticalContentAlignment="Center"
                              ItemsSource="{Binding SelectedChar.AvalibaleSkills}" x:Name="SkillCmb">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding ., Converter={StaticResource LocaleConverter}}" Padding="0" Margin="5,0,0,0"/>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                    <Button Grid.Column="2" Grid.Row="0" Height="20" Width="20" HorizontalAlignment="Right" Command="{Binding AddSkillCmd}" CommandParameter="{Binding ElementName=SkillCmb, Path=SelectedItem}" 
                            VerticalAlignment="Top" Margin="10,5,10,0">
                        <Button.Content>
                            <Image Source="/Resources/plus.png"/>
                        </Button.Content>
                    </Button>
                    <ScrollViewer Margin="5,-10,10,0"  Height="80" Padding="0" Grid.Row="1" Grid.ColumnSpan="3" CanContentScroll="True" >
                        <ItemsControl ItemsSource="{Binding SelectedChar.whiteTagsNEW}" >
                            <ItemsControl.ItemTemplate>
                                <DataTemplate DataType="local:WhiteTag">
                                    <Grid Margin="0,2,0,0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="85"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="{Binding id, Converter={StaticResource LocaleConverter}}" Margin="5,0" 
                                                   TextTrimming="CharacterEllipsis" HorizontalAlignment="Left" VerticalAlignment="Center" Padding="0"/>
                                        <TextBox Grid.Column="1" Text="{Binding value, StringFormat={}{0:0.00}}" Margin="0,0,5,0" VerticalAlignment="Center"
                                                 />
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>

                </Grid>
            </Border>
            <Button Grid.Row="6" Grid.Column="1"  Width="25" Height="25" IsEnabled="{Binding Save_Loaded}"
                    VerticalAlignment="Bottom" HorizontalAlignment="Right"
                    Command="{Binding SaveCmd}">
                <Image Source="/Resources/save.png"/>
                <Button.Style>
                    <Style>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Save_done}" Value="True">
                                <DataTrigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <ColorAnimation To="#7F00FF00" 
                                                            Storyboard.TargetProperty="(Button.Background).(SolidColorBrush.Color)" 
                                                            FillBehavior="Stop" AutoReverse="True" 
                                                            Duration="0:0:1"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </DataTrigger.EnterActions>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>


        </Grid>
        <!--<Border Grid.Row="1" Grid.ColumnSpan="2" Margin="-10,-25,-10,0" 
                VerticalAlignment="Center" Opacity="0">
            <Image  Source="/Resources/backgrnd.jpg" Stretch="UniformToFill" HorizontalAlignment="Center"/>
            <Border.Style>
                <Style TargetType="Border">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Save_Loaded}" Value="true">
                            <DataTrigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="Opacity" 
                                                         From="1" To="0" 
                                                         FillBehavior="HoldEnd" AutoReverse="False" 
                                                         Duration="0:0:1" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </DataTrigger.EnterActions>
                            <Setter Property="IsHitTestVisible" Value="False"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
        </Border>-->

    </Grid>
</Window>
